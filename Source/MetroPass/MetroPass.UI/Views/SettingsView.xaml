<UserControl
    x:Class="MetroPass.UI.Views.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MetroPass.UI.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid>
        <StackPanel Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Height="30" VerticalAlignment="Top">
                <TextBlock Text="Recycle Bin enabled: " VerticalAlignment="Center" FontSize="20" />
                <CheckBox x:Name="RecycleBinEnabled" VerticalAlignment="Top" Margin="10 0 0 0" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" Visibility="{Binding IsChecked, ElementName=RecycleBinEnabled, Converter={StaticResource BoolVisibilityConverter}}">
                <TextBlock Text="Recycle Bin:" FontSize="20" />
                <ComboBox DisplayMemberPath="Name"
                          ItemsSource="{Binding AvailableGroups}" 
                          SelectedItem="{Binding SelectedRecycleBinGroup, Mode=TwoWay}" />
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>